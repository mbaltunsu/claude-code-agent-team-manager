# Optional: override the central agents directory with a specific library path.
# If not set, the plugin auto-uses ~/.claude/team-management/agents/ (populated via
# /team:init-team download or /team:init-team import).
# AGENTS_LIBRARY_PATH=D:\CLAUDE_AGENTS\awesome-claude-code-subagents
